version: "3.8"

services:
  simplesamlphp:
    image: ilifesregistry.azurecr.io/simplesamlphp:latest
    ports:
      - "8081:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    external: true